<!DOCTYPE html>
<html>
  <head>
    <title>Project</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/foods">Le Recipe</a>
        <div class="d-flex justify-content-between">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 mx-5">
            <% if current_user %>
              <li class="nav-item">
                <%= link_to "Foods", '/foods', class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "My Recipes", '/recipes', class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Public Recipes", '/public_recipes', class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Shopping List", '', class: "nav-link" %>
              </li>
            <% end %>
          </ul>
          
        </div>
        <br>
          <% if current_user %>
            <div class="d-flex align-items-center">
              <p class="fs-6">Logged In: <Span class="font-weight-bold"><%= current_user.name %></Span></p>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link mx-5" %>
            </div>
          <% end %>
      </div>
    </nav>
    <br>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
